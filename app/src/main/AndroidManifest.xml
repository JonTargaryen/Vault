<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vault">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_lock"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_lock_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".genPassword_New"></activity>
        <activity android:name=".passList" />
        <activity android:name=".genPassword" />
        <activity android:name=".newPassword" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>